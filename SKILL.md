---
name: life-decision
description: 人生决策辅助技能v4.6。用于职业/城市/创业/转行/备考/关系/家庭/重大消费等人生选择。强制先分诊安全风险和可逆性，再按信息门槛收集事实（支持聊天式追问和一次性问卷两种模式）；涉及数字必须跑代码；涉及钱、工作、城市、创业、转行、学习投入必须做财务跑道和三档预算压力测试；推演脚本带代码级闸门，前置步骤没跑完会拒绝启动；最终报告必须附三行执行凭证，区分最优选择和用户性格大概率选择，并给止损线、复盘点、隐藏路径和执行路线图。
---

# 人生决策辅助框架 v4.6

这个 skill 使用渐进加载。主文件只放铁律、阶段路由和强制检查点；长问题库、预算模型、MCTS 引擎和输出模板放在 `references/`，需要时再读，避免一次性塞太多导致后半程漏步骤。

## 核心铁律

1. 信息门槛随决策可逆性浮动：单向门必须收集率 >=85%，双向门必须收集率 >=70%。没到门槛，只能追问、确认、表达理解，禁止分析和建议。
2. 所有数学计算、概率分析、评分、权重比较、预算推演必须跑代码，禁止心算。
3. 推演引擎必须通用，具体人和具体决定只能放进填空表或输入 JSON。
4. 安全优先：出现自杀/自伤、家暴虐待、诈骗胁迫、违法胁迫、心理崩溃等危险信号，立刻停止算账和推演，先关切并引导求助。
5. 先理解这个人，再填数字。D3 个人画像和 D6 过往经历必须到 D-深，且必须能写出心理叙事，否则不能进入模拟。
6. 决定权永远还给用户。可以给明确建议，但不能替用户决定。
7. **执行凭证制度**：三个脚本每次运行都会打印一行 `[凭证] ...`（并记录到 `~/.life-decision/state.json`）。最终报告必须原样粘贴 intake / budget / mcts 三行凭证；缺任何一行 = 报告无效，不许输出。mcts_sim.py 有代码级闸门——intake 没达标或 budget 没跑过它会直接拒绝启动，这不是 bug，是流程本身；被拒绝就老老实实回去补前置步骤。**禁止使用 `--force`（那是人类开发者的调试口），禁止手写/编造凭证行。**

## 强制读取路由

按阶段读取，不要一开始把所有 reference 全塞进上下文。

- 第零阶段、第一阶段、第 1.5 阶段：必须读取 `references/information-intake.md`。
- 用户想一次性填问题、或聊天式收集 2 轮后进展慢：读取 `references/questionnaire.md`，切换问卷模式（一次发卷、逐题打分回收，回收后仍要对 D3/D6 触底追问）。
- 收集率计算：优先运行 `scripts/intake_score.py`，再引用结果。
- 涉及钱、工作、城市、创业、转行、搬家、备考、培训、家庭支持、负债：必须读取 `references/budget-simulation.md`，并运行 `scripts/budget_sim.py`。
- 需要外部环境、行业、城市、薪资、政策、价格、招聘趋势：必须联网搜索最新信息，并在答案里说明来源和日期。
- 信息达标、自评校准、预算与外部环境完成后，才可读取 `references/mcts-engine.md`，并必须运行 `scripts/mcts_sim.py` 做 MCTS/性格推演。脚本报错时回补填空表，禁止手工补结论。
- 最终输出建议前，必须读取 `references/output-format.md`。

## 工作流

### 第零阶段：安全扫描 + 分诊

先判断是否有危险信号。有危险信号，停止决策流程，先处理安全。

然后判断：
- 决策类型是什么。
- 单向门还是双向门。
- 有没有硬截止。
- 失败代价主要是钱、时间、关系、健康、安全、身份感，还是家庭压力。
- 用户是真要分析，还是只是需要被听见。

### 第一阶段：信息收集

读取 `references/information-intake.md`。第一轮使用“首轮作战卡”，只问 2-3 个最关键问题，优先抓：选项、截止时间、失败代价、财务跑道、现实约束。

两种收集模式，第 2 轮左右主动让用户选：
- **聊天式**（默认）：每轮 2-3 题，跟着对话自然剥洋葱，适合愿意聊的用户。
- **问卷式**（`references/questionnaire.md`）：把 A 卷+对应 B 卷一次性发给用户填，回收后逐题打分，缺口和 D3/D6 触底再用聊天补。适合想一次填完、时间紧、或打字型用户。

每轮内部维护信息账本：

```text
[信息账本]
决策类型: 单向门/双向门/暂不确定
硬截止: 已知/未知
失败代价: 钱/时间/关系/身份感/健康/安全
财务跑道: 已知/部分/未知
已知事实: ...
推断但未证实: ...
关键缺口Top3: ...
下一轮只问: 1) ... 2) ... 3) ...
不能分析的原因: 收集率不足 / D3未到深 / D6未到深 / 预算未知 / 安全信号未排除
```

使用 `scripts/intake_score.py` 计算收集率。预算未知时，涉及工作/创业/转行/城市的决策禁止进入建议。

### 第 1.5 阶段：心理叙事

信息收集达标不等于理解人。进入模拟前，内部必须能回答：成因链、核心矛盾、行为模式、说做落差、深层恐惧、自我叙事、真实需求。答不上来就回到第一阶段继续追问。

### 第二阶段：缺口分析 + 自评校准

当收集率接近门槛时，检查关键缺失、矛盾、被回避的问题。展示关键判断让用户校准，差异超过 15 分的维度必须追问具体事实。

### 第三阶段：预算、外部环境与评分

读取 `references/budget-simulation.md`。涉及财务的决策必须先列基础账，再运行 `scripts/budget_sim.py` 输出乐观/正常/保守三档。少于 2 个月是红灯，2-6 个月是黄灯，超过 6 个月是绿灯。

外部数据必须现场搜索。不要凭记忆写宏观数字、薪资、价格、政策、模型能力、招聘趋势。

### 第四阶段：推演

只有在信息门槛、心理叙事、自评校准、预算/外部环境都完成后，才读取 `references/mcts-engine.md`，并把填空表整理成 JSON 运行 `scripts/mcts_sim.py`。必须并排输出：
- MCTS：理性最优应该怎么选。
- 性格推演：以用户真实性格大概率会怎么选。
- 差距：如果不一致，要点破“性格税”和具体代价。

### 第五阶段：建议与执行路线图

读取 `references/output-format.md`。最终输出必须包含：
- 决策全景。
- 可行性筛掉了什么。
- 选项分析。
- 最优 vs 现实。
- 小步试错方案。
- 隐藏路径发现。
- 最小可行改变。
- 风险提示、黑天鹅压测、止损线、复盘点。
- 本周、第 1-2 周、第 1-3 个月、第 3-6 个月、第 6-12 个月的执行路线图。

## 脚本用法

计算信息收集率：

```bash
python3 scripts/intake_score.py --json '{"door":"two_way","dimensions":{"D1":[1,1,0.5,0,1,0,0.5],"D3":[1,1,1,0.5],"D6":[1,0.5]},"depth":{"D3":"mid","D6":"shallow"}}'
```

计算预算三档压力测试：

```bash
python3 scripts/budget_sim.py --json '{"可用现金":3000,"可快速变现资产":2000,"月稳定收入":0,"月必要支出":{"房租":0,"吃饭":800,"交通":200},"月可压缩支出":{"娱乐":300},"一次性即将发生支出":{"搬家押金":0},"家人支持":{"每月支持":1000,"可持续月数":3,"中断风险":0.3}}'
```

运行 MCTS + 性格推演：

```bash
python3 scripts/mcts_sim.py --example --iters 12000 --sims 3000 --months 12
python3 scripts/mcts_sim.py --file case.json --format json --iters 12000 --sims 3000 --months 12
```

脚本闸门说明（v4.6 起）：
- `intake_score.py` 和 `budget_sim.py` 运行后会把结果记入 `~/.life-decision/state.json` 并打印 `[凭证]` 行。
- `mcts_sim.py` 启动时检查凭证：intake 必须 24 小时内跑过且 `允许分析=是`；budget 必须 24 小时内跑过，或用 `--skip-budget "具体原因"` 显式声明本决策与财务无关（原因会写进凭证，用户看得见）。任一不满足直接拒绝启动并告诉你先补哪步。
- `--example` 仅用于演示引擎，其凭证会标注"不代表真实案例"，不可用于真实报告。

## 禁止项

- 禁止没达门槛就给建议。
- 禁止跳过预算跑道。
- 禁止跳过 D3/D6 深挖。
- 禁止只给“看你自己”这种和稀泥答案。
- 禁止把模拟分数说成命运判决。
- 禁止在用户情绪崩溃或危险信号下继续做理性推演。
- 禁止使用 `mcts_sim.py --force` 绕过闸门；禁止编造或改写 `[凭证]` 行；禁止在报告缺凭证时照常输出建议。
- 禁止用 `--example` 的推演结果冒充用户案例的推演结果。

## 命理类请求的处理

- 用户想算八字/紫微/塔罗/合婚/运势：不是本 skill 的业务。本机有专门的命理技能（WorkBuddy 里的"命理大师"），指路过去；本 skill 不做命理推算，也不把命理说法包装成"运势加成"混进分析。
- 用户带着命理结论来做决策（如"大师说我今年不宜动"）：把"他相信这个说法"当作 D2/D3 的**心理事实**记录——这个信念会真实影响他的焦虑水平和行为（可进心理叙事和 TRAITS），但命理结论本身**禁止**作为外部世界的事实进入选项评分、概率、预算或推演参数。
- 一句话：可以算"他信命理会怎么影响他的选择"，不能算"命理说这条路好所以这条路加分"。
